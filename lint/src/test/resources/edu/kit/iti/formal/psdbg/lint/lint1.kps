script A () {

}

script A () { // not unique identifier

}


script B () {
    theonly { theonly {} }
    foreach { foreach {} }
    repeat { repeat {} }
    theonly { foreach {} }
    foreach { theonly {
        cases {
            case not match `f(X)` using [X:int] {}
            case not match `f(y)` {}
        }
    } }
}